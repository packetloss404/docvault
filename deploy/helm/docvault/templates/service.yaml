apiVersion: v1
kind: Service
metadata:
  name: {{ include "docvault.fullname" . }}
  labels:
    {{- include "docvault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docvault.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
